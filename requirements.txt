# PDF to Markdown Converter Dependencies

# Core conversion library (Microsoft)
markitdown>=0.1.0

# Optional: Drag & Drop support for Windows
tkinterdnd2>=0.3.0

# For EXE building
pyinstaller>=6.0.0
